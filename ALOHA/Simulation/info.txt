file name     ------   experiment performed

Transceiver_Async --  here we tried to implement Async crc32 and correlate access code stock block

Tx_PDUexp         --  single transmitter chain experimented on converting PDU before sending to channel

Rx_ZMQ            --  receiver chain with packet decoding using ZMQ server

Tx_ZMQ_collision  --  in this flow graph we are trying to superimpose complex baseband signals; since the dsp blocks work on sequence based indexing it always leads to collision

Tx_ZMQ_collision_exp -- in this flow graph we are not trying to superimpose complex baseband signals but Packet-drop event instead of complex baseband; since the dsp blocks work on sequence based indexing it always leads to collision; 

Transceiver (keyfile) ---  in this flow-graph we created a custom block to overcome sequence based indexing of DSP blocks

aloha_stats.py      ----   to compute the per, ber, S vs G for all received packets (all users)

